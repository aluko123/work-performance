

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./bert_classification:/app/bert_classification
      - ./data:/app/data

  frontend:
    build: ./frontend
    ports:
      - "8001:80"
    depends_on:
      - backend
