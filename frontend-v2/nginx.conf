server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri /index.html;
  }

  # Optionally pass through API routes to backend if needed in prod behind same proxy
  # location /api/ {
  #   proxy_pass http://backend:8000;
  # }
}

